#!/usr/bin/env python3
"""
🧒 KIDO Programming Language — Launcher
This is the only command users ever see.
"""
import sys
import os

# Add KIDO SDK to path (relative to this script)
script_dir = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, script_dir)

# Also check installed location
if os.path.exists("/usr/local/lib/kido"):
    sys.path.insert(0, "/usr/local/lib/kido")

from kido_cli.cli import main

if __name__ == '__main__':
    main()
